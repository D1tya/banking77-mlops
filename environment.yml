name: banking77
channels:
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - pip:
      - pandas
      - numpy
      - scikit-learn
      - xgboost
      - mlflow
      - optuna
      - pandera
      - pydantic
      - fastapi
      - uvicorn
      - sqlalchemy
      - psycopg2-binary
      - python-dotenv
      - joblib
      - pytest
      - httpx
      - prometheus-client
      - evidently
